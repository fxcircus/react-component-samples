import React from 'react';
import './Divider.css'; // Import the CSS file

const Divider: React.FC = () => {
  return <div className="divider"></div>; // Use the CSS class
};

export default Divider;
